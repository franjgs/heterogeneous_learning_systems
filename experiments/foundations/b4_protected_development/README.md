# B4 Protected Portfolio Development

See `results/foundations/b4_protected_development/README.md` for the frozen
Phase-A implementation status. The default runner mode is a training-free dry
run. A scientific run requires the explicit `--run-full` flag and remains
unauthorized during Phase A.
