# Paper figures

The paper includes diagnostic PDF figures generated reproducibly by
`experiments/microverification/verify_minimal_theory.py` and stored in
`results/microverification/`. They are referenced from `paper/main.tex` through
relative paths so the paper does not duplicate generated artifacts.
