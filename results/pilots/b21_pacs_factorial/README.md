# B2.1 output directory

This directory is intentionally empty of experimental results until the
factorial is launched manually. The runner will write `factorial_results.csv`,
`state_scores.csv`, `aggregates.csv`, and `protocol.json` here. No TEST metrics
are produced by B2.1.
