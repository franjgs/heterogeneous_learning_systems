# Foundation results

Reserved for reviewed outputs of `experiments/foundations/`.  Each future
experiment receives a matching directory with a provenance `manifest.json` and,
when appropriate, versioned figures, tables, and metrics.  Caches and temporary
logs do not belong here.
