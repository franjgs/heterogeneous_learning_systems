# B5 Gradient-Protected Development

Status: **IMPLEMENTED — FULL RUN NOT EXECUTED**.

TEST status: `PREVIOUSLY_OPENED_NOT_USED_IN_B5`. GREP uses exactly the frozen B2.4 F0, opportunity, replay, schedule and optimizer. VALIDATION is evaluated once after each completed fit and never participates in training. The default command is a training-free dry run; `--run-full` is explicit.
